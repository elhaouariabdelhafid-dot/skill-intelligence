# ===== Phases 1-2 : RAG =====
qdrant-client>=1.12,<2
sentence-transformers>=3.0        # BGE-M3 + reranker (CrossEncoder)
FlagEmbedding>=1.3                # BGE-M3 natif (dense + sparse)
rank-bm25>=0.2.2
langchain-text-splitters>=0.3     # splitters seuls, sans tout LangChain
pymupdf>=1.24                     # parsing des whitepapers PDF
tqdm>=4.66
python-dotenv>=1.0
pydantic>=2.7
pydantic-settings>=2.3
requests>=2.32

# ===== LLM =====
ollama>=0.3
anthropic>=0.34
openai>=1.40

# ===== Évaluation (Phase 2) =====
ragas>=0.2,<0.3
datasets>=2.20
scipy>=1.13
pandas>=2.2
numpy>=1.26

# ===== DB (utilisé à partir de la Phase 3, installé dès maintenant) =====
sqlalchemy>=2.0
psycopg2-binary>=2.9

# ===== Observabilité =====
langfuse>=2.50,<3
